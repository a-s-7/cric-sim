name: Match Update Agent

on:
  schedule:
    - cron: '0 0,5,10,15,20 * * *'
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Trigger match update and wait
        run: |
          curl --max-time 300 \
               --retry 2 \
               --retry-delay 10 \
               -X POST \
               -f \
               https://cric-sim.onrender.com/run-match-update